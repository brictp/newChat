<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Protected</title>

    <style>
      *,
      *::before,
      *::after {
        box-sizing: border-box;
      }

      body {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue",
          sans-serif;
        background-color: #f5f5f5;
        height: 90vh;
      }

      p {
        margin-bottom: 25px;
        font-size: 18px;
      }

      a {
        display: block;
        margin-top: 20px;
        padding: 8px;
        width: 60px;
        background-color: #007bff;
        color: #fff;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        text-decoration: none;
      }

      a:hover {
        background-color: #0069d9;
      }
    </style>
  </head>

  <body>
    <a id="span" href="/">Volver</a>

    <h1>Hola <%= username %>!</h1>
    <p>
      Este contenido está protegido, Necesitas tener permisos para acceder a
      esta informacion.
    </p>
  </body>
</html>
